# VERIFICATION — 2026-07-31-phase2-wayfinder

Running log for the Phase 2 wayfinder chart. Canonical artifact is the GitHub map ([#9](https://github.com/NotTheRealRohit/study-planner-web/issues/9)); this log tracks charting progress.

## Status

| Item | State |
|---|---|
| Map created (#9) | ✅ done |
| Frontier tickets #10–#17 | ✅ created (8) |
| Blocked tickets #18–#21 | ✅ created (4), wired |
| Research: open-notebook | ✅ `research/external/open-notebook-findings.md` |
| Research: code sandbox (#16) | ✅ resolved + closed — Judge0 CE |
| Execution order (waves) | ✅ set 2026-07-31 — see PLAN.md "Execution order" |
| Decision tickets #10–#15, #17–#21 | ☐ open (one per future session) |
| #10 App IA & navigation | ✅ resolved + closed 2026-07-31 |
| #11 Assessment types, formats & scoring | ✅ resolved + closed 2026-08-01 |
| #15 Persistence & local-first fit | ✅ resolved + closed 2026-08-01 — **Wave 0 complete** |
| #17 Inline-hint live guide (prototype) | ✅ resolved + closed 2026-08-01 — **Variant C**; **Wave 1 complete** |
| #14 AI backend home & streaming | ✅ resolved + closed 2026-08-01 — Gemini free tier (cost-principle reversed); fetch-stream SSE; anchored-artifact abuse hardening |

## Log

- **2026-07-31** Charted the Phase 2 wayfinder map on GitHub Issues (repo `NotTheRealRohit/study-planner-web`). Named destination via grilling = **spec + working prototypes**; established the **capstone / feature-rich / no-deferrals** standing principle. Locked 7 design decisions (guidance style, trigger, inline-hint surface, full-RAG grounding, full KT coupling, hybrid code execution, out-of-scope fence). Created wayfinder labels, map **#9**, 8 frontier tickets **#10–#17**, 4 blocked tickets **#18–#21** (blocking via `Blocked by:` body refs). Fired two research subagents: open-notebook reuse (report at `research/external/open-notebook-findings.md`; verdict = port patterns, don't run the service) and code-sandbox comparison.
- **2026-07-31** Resolved research ticket **#16** (Code execution sandbox selection): **self-host Judge0 CE** over HTTP (native grading verdicts, 90+ langs, `isolate` isolation; keep over-the-wire re GPL-3.0). Piston = fallback. Report at `research/external/code-sandbox-comparison.md`; resolution comment posted; issue closed; decision recorded on map #9.
  Next: work the frontier — suggested order is the infra trio (#12 content storage → #13 vector store → #14 AI backend, which unblock #18), or build the #17 inline-hint prototype first. Run `/wayfinder 9`.
- **2026-07-31** Set the tackle order as 4 waves (see PLAN.md "Execution order"): Wave 0 frame (#10 → #11 → #15), Wave 1 de-risk (#17 prototype, parallel), Wave 2 AI infra trio (#14 → #12 → #13, unblocks #18), Wave 3 downstream (#18 → #19 → #20 → #21). Rationale: blocking graph + prototype the fuzzy #17 guide early. Each frontier ticket tagged with its wave in the ticket tables.
  Next: **Wave 0 → #10 App IA & navigation**. Run `/wayfinder 9 10` (or `/wayfinder 9` to auto-pick). Parallel tracks available: #10/#11 · #17 · #14→#12→#13.
- **2026-07-31** Resolved Wave-0 ticket **#10 App IA & navigation** (grilling + domain-modeling, grounded in `App.tsx`, `NavBar.tsx`, `AppShell.tsx`, roadmap-engine types). Five decisions: (1) **two top-level tabs** Assessments + Practice, each a Roadmaps-style hub→detail dashboard; (2) items are **material-scoped, 1+ materials** — the selected set is the RAG grounding source (via #12 extracted content) and the KT mastery target (mastery updates each selected material); (3) mobile bottom bar **demotes Settings to a header/profile menu** → clean 6-tab bar (Home/Session/Week/Roadmaps/Assessments/Practice), desktop shows all; (4) **nested path-param routes** `/assessments`, `/assessments/new`, `/assessments/:id` (same for `/practice`) under the `/study` basename per `react-router-v7-basename`; (5) inside `ProtectedRoute→RequireOnboarding→AppShell` — records are **local-first** new event kinds (Dexie + existing SyncEngine), generation is **online-only** via the Intelligence Service (offline = review/redo cached). Resolution comment posted; #10 closed; decision recorded on map #9; new fog added (Material library/detail surface).
  Next: **Wave 0 continues** — #11 Assessment types/formats/scoring and #15 Persistence & local-first fit are both independent and takeable in parallel sessions. Run `/wayfinder 9 11` then `/wayfinder 9 15`. Wave-1 #17 prototype and the Wave-2 infra chain (#14→#12→#13) can also run concurrently. Frontier query: `gh issue list --label wayfinder:phase2 --state open`.
- **2026-08-01** Resolved Wave-0 ticket **#11 Assessment types, formats & scoring** (grilling + domain-modeling, grounded in roadmap-engine `Material`, app event kinds, `packages/progress`, and the open-notebook transformations note). Four decisions: (1) **the Question is the gradeable atom**; an Assessment is a material-scoped container of possibly-mixed-format questions — quiz/written/coding are question *families*, not the top-level tab (#10) nor a per-assessment type; Practice (#21) reuses the same Question primitives coached. (2) Three families with a closed `format` enum, all formats in scope (capstone breadth): **objective** = mcq, multi_select, true_false, cloze, numeric (auto-graded); **written** = short_answer, long_form (LLM rubric); **coding** = implement_fn, debug, output_prediction, complete_code (Judge0 CE #16; output_prediction objective-graded). (3) **Scoring contract**: each family scores its own way but every graded attempt reduces to a normalized `score∈[0,1]` **plus** a per-skill binary `correct` (`score ≥ τ`, default ≈0.6) — the `perSkill` tuple is the KT observation #19/#20 consume; the exact score→mastery math and final τ stay #19's job. Typed schema pinned for `Assessment` / `QuestionBase` + per-format discriminated payloads / `Attempt` / `GradedResult` (see resolution comment on #11). (4) **Difficulty** = authored ordinal **1..5** band (Bloom-ish) set by #18, plus a reserved-but-empty `observedDifficulty {b?, pCorrect?, n?}` slot #20 fills from attempt data without a migration — adaptive from the first session, refined over time. Resolution comment posted; #11 closed; decision recorded on map #9; new fog added (**Assessment feedback / review surface**).
  Next: **Wave 0 → #15 Persistence & local-first fit** — the last independent Wave-0 ticket (the event/data-model spine every record must obey; blocks #19 with #11). Run `/wayfinder 9 15`. Wave-1 #17 prototype and the Wave-2 infra chain (#14→#12→#13) remain concurrently takeable. Frontier query: `gh issue list --label wayfinder:phase2 --state open`.
- **2026-08-01** Resolved Wave-0 ticket **#15 Persistence & local-first fit** (grilling + domain-modeling, grounded in `EventStore.ts`, `EventStoreProvider.tsx`, `SyncEngine.ts`/`sync/types.ts`, migration `003`, and the eventstore/dexie/sync/supabase rules). **Correction found:** the live Dexie schema is **v5** (tables `events, sync_queue, sync_meta, onboardingDraft, activeSession, calibrationCache`), not v3 as `eventstore-architecture` claims — so the migration is **v5→v6**. Load-bearing constraint: the snapshot is a single JSON blob of the entire event log capped at **5 MB**, and Supabase `events` is the one source of truth. Five decisions: (1) **Content home = three-layer split** — thin local-first `AssessmentCreated` pointer event (synced spine) + server-owned Supabase `assessments`/`questions` rows (fat generated content incl. hidden answers/solutions; migration `004`, owned by #18) + non-synced Dexie `assessmentContentCache` (redacted/answer-free, offline read). Keeps the event log + snapshot lean; answer keys never leave the server. (2) **Attempt grain = per-Question, runId-grouped** — `QuestionAttempted`→`QuestionGraded` (1:1 via `attemptId`; `perSkill[]` = one clean KT observation per event, honoring #11's Question-atom); **retry = a fresh observation** (new `attemptId`); optional `AssessmentRunCompleted` summary. Finalizes #10's suggested Assessment-level kinds into question-grained ones. (3) **Mastery = derived projection cached in `masteryCache`** (calibrationCache precedent, keyed by `materialId`, non-synced), recomputable from synced `QuestionGraded.perSkill`; `MasteryUpdated` event **reserved** (not required) so #19 can opt into an audit trail without a migration. (4) **All grading server-side, no offline grading** (user chose the max-integrity/consistency option over the stakes-split recommendation) — redacted cache never holds answers for any surface; `QuestionAttempted` queues offline, `QuestionGraded` completes on reconnect (Practice trades instant offline feedback for integrity). (5) **Dexie v5→v6** adds only the two non-synced cache tables (`assessmentContentCache: 'assessmentId'`, `masteryCache: 'materialId'`); new event kinds are rows in the existing `events` table; practice-resume tables (`activePracticeSession`, `HintShown`) reserved to #21/#17. Resolution comment posted; #15 closed; decision recorded on map #9; new fog added (**Content cache lifecycle / eviction**). **#15 + #11 together unblock #19.**
  Next: **Wave 0 is complete.** Frontier = **Wave-1 #17 Inline-hint live guide (prototype)** — the highest-uncertainty centerpiece, unblocks #21 — and the **Wave-2 infra chain #14→#12→#13** (unblocks #18). #19 is now unblocked (#11+#15 done) but placed in Wave 3 to follow the generation shape. Run `/wayfinder 9 17` (prototype) or `/wayfinder 9 14`. Frontier query: `gh issue list --label wayfinder:phase2 --state open`.
- **2026-08-01** Resolved Wave-1 ticket **#17 Inline-hint live guide (prototype)** — the highest-uncertainty centerpiece, carrying the map's **execution override** (build a working throwaway prototype, not just a decision). Built via the `/prototype` skill: a DEV-only route `/study/practice-prototype` (mirrors the `/chart-test` precedent; `import.meta.env.DEV`-gated) with **three structurally-different surfaces** for the inline-hint guide, all obeying the locked contract (tiered-Socratic, hybrid on-demand triggers, hint-never-answer): **A** inline ghost text pinned to the active line, **B** margin coach rail with a vertical Socratic stepper trail, **C** anchored coach popover. Scripted hint ladders (no model) for a coding + a written example; final reveal tier gated. **Verification:** app `typecheck` clean; Playwright drove each surface through the trigger→accept→escalate→gated-reveal flow and screenshotted them (inspected). **HITL decision — Variant C chosen** by the user from live use, with two feedback rounds folded in: (1) hints must be **line-anchored + line-aware** — anchor follows the caret, leads with the line it reads ("Looking at line 4 `cleaned += c`"); (2) hint copy must **orient-then-ask** and escalate in concreteness (Socratic ≠ cryptic riddles). Settled for #21: surface C, 4-tier ladder `nudge→hint→targeted→gated-reveal` with structural reveal-gating, three hybrid triggers (I'm stuck / idle / failed-test that OFFER), and a tier-directness/count tuning dial. Guide mechanism = a **streamed server-side Intelligence-Service LLM call** (question + learner answer/code + active-line context + RAG chunks + tier); "never hands over" enforced in prompt **and** structurally (reveal is a separate gated call); **model choice TBD = #14**. Prototype limitation → #21 build note: real Practice editor needs **Monaco/CodeMirror** inline-widget API (a `<textarea>` can't push lines). Prototype captured as primary source on throwaway branch **`prototype/wf17-inline-hint-guide`** (`df0d2ab`, pushed; not for mainline — the working branch keeps only the validated decision). Resolution comment posted; #17 closed; decision recorded on map #9; #21 moved to the unblocked frontier; fog updated (guide tuning dials + editor choice now #21 inputs).
  Next: **hand off #14 AI backend home & streaming** (Wave 2; picks the model + streaming transport the #17 guide assumes; unblocks the #12→#13→#18 chain), or **#21 Practice session model** (now unblocked by #17). Run `/wayfinder 9 14` or `/wayfinder 9 21`. Frontier query: `gh issue list --label wayfinder:phase2 --state open`.
- **2026-08-01** Resolved Wave-2 ticket **#14 AI backend home & streaming** (grilling + domain-modeling, grounded in `services/intelligence` [`main.py`, `security.py`, `pyproject.toml`], the `claude-api` skill, the Gemini pricing/models/embeddings docs, and open-notebook findings §C/§D). **Major reversal surfaced mid-grill:** the learner has **no funds for Claude**, so the map's standing *"development cost is not a constraint"* principle is **reversed for the model layer only** — new principle = **cost-effective, accurate-enough**; feature scope stays full. Five decisions: (1) **Single JWT-verified Intelligence Service** owns all model + embedding calls (confirmed); #14/#13 seam = #14 owns the embedding client, #13 bulk-drives it; query-time embed is sync on the RAG hot path, ingest-time embed is #13's async worker. (2) **Provider = Google Gemini free tier** via the official `google-genai` SDK behind a thin provider abstraction (`generate`/`stream`/`embed`) with **local Ollama** as the swappable fallback: **guide** = `gemini-3.5-flash-lite` (latency-first), **generation + grading** = `gemini-3.6-flash`, **embeddings** = `gemini-embedding-001` @ **768-dim** with `task_type` asymmetric (all confirmed free-tier). Accepted tradeoff on record: free-tier data is used to improve Google's products → dissertation ethics note. (3) **Streaming** = `POST` + `fetch` ReadableStream over the JWT `/v1/*` router (**not** `EventSource` — header auth), provider-neutral SSE frames (`delta`/`question`/`done`/`error`); **guide** streams token deltas with **reveal a separate gated endpoint** (`/v1/guide/reveal`, the only call that loads the answer key), **generation** streams one validated `Question` per frame, **grading** stays request/response. (4) **Prompt architecture** = git-versioned Jinja templates in `app/prompts/*.jinja` (ported open-notebook `execute.jinja`/ai-prompter; `{{format_instructions}}`→native `responseSchema`); **SSTI rule** (untrusted text = render variables, never compiled); untrusted-as-data delimiting + injection-guard preamble; **answer key excluded from hint context**. (5) **Keys/JWT + abuse hardening** = key in `services/intelligence/.env` server-side only; **hard rule — no endpoint runs the model on arbitrary user text; every LLM call carries typed size-capped fields bound to a persisted, user-owned artifact** (ownership-checked pre-call); defense-in-depth = JWT boundary · ownership authz · typed+capped input · delimited untrusted data · answer-key exclusion · `responseSchema` output · **durable Postgres-backed per-user + per-endpoint rate limits + global free-tier circuit breaker** (graceful 429; grading queues offline per #15) · audit logging. UX cost to honest users = **zero** (the surface is a practice editor with a summonable coach, not a chat box). Resolution comment posted; #14 closed; decision + cost-principle reversal recorded on map #9 (Notes amended) and PLAN; **fog "Prompt architecture & prompt-injection safety" graduated/resolved**; new capstone-eval candidate metric noted ("grader robustness to adversarial submissions"). New service deps flagged: `google-genai`, `jinja2`, an SSE mechanism, a Postgres client.
  Next: **hand off #12 Content ingestion & storage** (Wave 2; the content that gets chunked — fixes the title-only material gap; unblocks #13, then together they unblock #18). Then **#13 Vector store, embeddings & async jobs** (consumes #12; wires `gemini-embedding-001`@768-dim into pgvector + the async worker that bulk-drives #14's embedding client). Run `/wayfinder 9 12`. Frontier query: `gh issue list --label wayfinder:phase2 --state open`.
