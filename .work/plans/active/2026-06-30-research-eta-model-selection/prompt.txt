You are a research/ML engineer RESUMING a pre-written, verified implementation plan that is already in
progress. Your single goal: complete the remaining phased research work in PLAN.md — extend the offline
harness for the new material/session design, re-validate calibration & detection, and BENCHMARK/SELECT the
finish-date (ETA) model — while maintaining the continuous scratchpad so the work survives across sessions
and the reviewer can audit it. You implement and report; you do NOT decide the work is "done" — a separate
reviewer signs each phase off.

=================================================================================================
RESUME STATE — READ THIS FIRST (where the work stands as of 2026-06-30)
=================================================================================================
- P0 is COMPLETE and reviewer-✅ (including the corrective P0b). Do NOT redo it.
- R1 is COMPLETE and reviewer-✅ (2026-06-30). The decoupled dataset exists and was invariant-verified
  over all 5400 learners: `research/datasets/synthetic-decoupled-9e6d48db2da8-seed0-n5400`
  (regime `decoupled`, generator_version 0.2.0, base hash 21c2cdabfa91 + decoupled hash a99216b83551,
  9 archetypes × 3 bands × 200 = 5400). This is the dataset R3/R4 run on. Do NOT regenerate it.
- R2 is COMPLETE and reviewer-✅ (2026-06-30). Calibration win TRANSFERS on decoupled data with partials
  included (context-pred 12/12 Holm wins, recovery 8/12) — result in `research/results/calibration_decoupled/`.
  Do NOT redo R2.
- R3 is COMPLETE and reviewer-✅ (2026-06-30). Detection under the decoupled cadence is MORE CUSUM-favoring:
  drift→cusum AND step→cusum (the A4/P0b step→page_hinkley edge disappears); only `fading_flame` drift
  cells retain Holm-surviving `csd`/`page_hinkley` wins. Result in `research/results/detection_decoupled/`.
- R4 is COMPLETE and reviewer-✅ (2026-06-30, the headline). VERDICT: the #3 composite `gp_plus_analytic`
  beats `gp_ard` under held-out+Holm on the SMALL band ONLY (cold-start/low-data regime); it is worse on
  max; `analytic_required_rate` never wins. So **#3 is QUALIFIED, not a general win** — a cold-start
  fallback layered on GP, NOT a GP replacement (gp_ard/conformal stay better on data-rich plans; conformal
  is the coverage fix). Result in `research/results/projection_decoupled/`. Do NOT redo R4.
- The authoritative A-series PARITY BASELINE is on disk in the DEFAULT result dirs
  (research/results/{calibration,detection,projection}/*.json), regenerated with current code from the
  frozen dataset `research/datasets/synthetic-21c2cdabfa91-seed0-n5400` (9 archetypes × 3 bands × 200 =
  5400; 5-train/4-held). **D-09 supersedes D-08.** These default-dir results are your COMPARISON ANCHOR
  for R2/R4/R5 — do NOT clobber them (decoupled runs go to SEPARATE `--out-dir`s).
- A safe baseline-completion code fix already landed: `_index_to_date()` in
  research/comparison/src/research_comparison/baselines/projection.py clamps non-finite / out-of-range
  day offsets to date.min/date.max (with a regression test). Keep it; the R4 analytic candidate relies on it.

START-OF-SESSION STEP 0 (do this BEFORE any R5/R6 work): the reviewer left uncommitted doc changes in the
working tree (R3 + R4 flipped to ✅ in VERIFICATION.md, status line, and this prompt.txt). COMMIT them
first so the history is clean before you begin, e.g.:
  git add .work/plans/active/2026-06-30-research-eta-model-selection/
  git commit -m "docs(research): record R3/R4 reviewer sign-off; point prompt at R5/R6"
(Commit ONLY the plan-dir docs; leave the other unrelated dirty working-tree items alone.)

>>> YOUR NEXT PHASES ARE R5 (rigour parity + claims-ledger) then R6 (N=1 real-data guard). Do R5 FIRST
    (R6 builds on the honest framings R5 writes). Both are mostly docs — no new benchmarks. <<<

TWO REVIEW CARRY-FORWARDS you MUST honor (from the P0b sign-off):
1. NAME: the dual-prior calibrator is registered as `enriched_dual_prior` (NOT `dual_prior` as some PLAN
   prose says). Use `enriched_dual_prior` everywhere in R2/R4/R5 comparisons and write-ups.
2. DETECTION BASELINE is NOT a pure robust-null on this lineage: current code gives drift→`cusum` but
   step→`page_hinkley`, with `csd`/`page_hinkley` Holm-surviving vs `cusum` on `fading_flame` cells (this
   matches the A4 result). When you reach R3, state your verdict against THIS baseline, not a
   "nothing beats CUSUM" strawman.
PROCESS: leave each finished phase at 🟡 and let the REVIEWER flip 🟡→✅ (P0 was pre-flipped; don't repeat
that pattern).

CONTEXT
- Repo root: /Users/rsaji/projects/1/college-mtech/study-planner-web  (branch: project/phase-1)
- The plan and its companions live in:
  .work/plans/active/2026-06-30-research-eta-model-selection/
    PLAN.md          ← the contract you execute (phases P0 ✅ → R1 → (R2 ∥ R3 ∥ R4) → R5 → R6)
    VERIFICATION.md  ← formal per-phase acceptance + reviewer sign-off (P0 ✅ recorded)
    SCRATCHPAD.md    ← your continuous working memory (P0/P0b logged; resume from §2 "Next actions")
- Design source of truth (read fully before R1):
  .work/plans/active/2026-06-30-material-session-decoupling/DECISIONS.md  (D8, D8a, D9, D9a, D10, §5c)
- The harness you extend: research/comparison/  (a `uv` workspace package `research-comparison`,
  run from repo root via the Makefile / `uv run --package research-comparison python -m ...`).
- This is an OFFLINE research workstream — it must not depend on any apps/ or packages/ change.

SOURCES & TOOLS (all complementary — combine them; none is a substitute for another)
- PLAN.md — USE FOR: every phase's exact files, run commands, ground-truth specs, acceptance
  criteria, and the Decisions log (D-01…D-09). Follow it. NOT FOR: a place to write — it is
  read-only to you. [MANDATORY]
- DECISIONS.md — USE FOR: WHY the design changed and what each locked decision means. NOT FOR:
  file/line anchors (those are in PLAN §3). [MANDATORY to read once]
- The live harness code under research/comparison/ — USE FOR: the real, authoritative file/symbol
  truth; verify each PLAN §3 anchor against it before you rely on it. NOT FOR: assuming it matches
  PLAN if it doesn't — if reality differs, fix course and log it. [MANDATORY]
- SCRATCHPAD.md — USE FOR: writing your running state every session (status, next actions, decisions,
  numbers, deviations, blockers, append-only session log). [MANDATORY to maintain]
- VERIFICATION.md — USE FOR: filling the "Developer notes" block per phase and flipping its marker
  ☐→🟡 when implemented. NOT FOR: marking ✅ — only the reviewer does that. [MANDATORY]

MANDATORY RULES (these override any shortcut implied later)
1. YOU own all git; commit after each phase and record the SHA in VERIFICATION.md + SCRATCHPAD.md.
   Never push unless asked. Work on branch project/phase-1. (Step-0 plan-doc commit is already done.)
2. Write ONLY under: research/**, college/scope/decoupled-session-preregistration.md (the new pre-reg
   doc you author), and the plan-dir docs. Do NOT touch apps/ packages/ e2e/ services/ — this is
   offline research. Leave the unrelated dirty working-tree items alone (STATUS.md, the skill file,
   handovers, the decoupling plan).
3. NEVER overwrite or clobber the A-series: do not change PARAMS_VERSION_HASH (don't edit
   college/scope/archetype-preregistration.md), do not modify the hard-coded reference dataset ids in
   research/comparison/src/research_comparison/runners/calibration.py:49-50, and run every DECOUPLED
   track with a SEPARATE `--out-dir` so the frozen P0 baseline in
   research/results/{calibration,detection,projection}/ (your comparison anchor) stays intact (PLAN D-01).
   New data is a new `decoupled` regime (PLAN R1).
4. NO FABRICATION / honest results: report the real numbers from the stamped result JSONs; only
   Holm-surviving improvements count as "wins"; if a "transfer" result degrades, that's a finding —
   apply the pre-declared fallback and record it. Never present a guess as fact.
5. UPDATE SCRATCHPAD.md at the start (set "Next actions") and end (append a Session-log entry) of
   EVERY work session, because this is a multi-session task and lost context = repeated/contradictory
   work.
6. A phase is NOT done until the reviewer flips its VERIFICATION.md section to ✅. You may reach 🟡
   (implemented, awaiting review) and then move on to an independent phase, but do not self-certify
   and do not pre-flip ✅ yourself.

WORKFLOW
Remaining phases in dependency order: R1 → (R2, R3, R4 — independent, any order) → R5 → R6.
The scheduling track is OUT — do not run or extend it. (The 2 pre-existing closed_loop test failures
are in that retired track and are out of scope — leave them.)

For EACH phase, run these steps in order:
1. Read the phase section in PLAN.md + its VERIFICATION.md acceptance criteria. Set SCRATCHPAD §2
   "Next actions" for this phase.
2. Verify the PLAN §3 file/symbol anchors the phase touches against the live code. If anything
   differs, adjust and log it in SCRATCHPAD §4 + §6.
3. Implement. Smoke-test with a tiny `--seeds 4` run before any full `--seeds 200` run.
4. Run the track per the PLAN's exact commands (DECOUPLED runs → SEPARATE `--out-dir`; never overwrite
   the frozen P0 baseline). Capture the real headline numbers into SCRATCHPAD §5.
5. Write/extend tests under research/comparison/tests/ as the PLAN's acceptance lists require. Run
   what the sandbox allows; record run-vs-authored-only honestly in SCRATCHPAD §3.
6. Commit. Fill VERIFICATION.md "Developer notes" (files, SHA, what ran, deviations, evidence) and
   flip the phase marker ☐→🟡 (NOT ✅). Append a SCRATCHPAD §8 session-log entry.
7. Pause for reviewer sign-off if the next phase depends on this one; otherwise proceed to an
   independent phase.

IMMEDIATE NEXT ACTIONS — R5 (rigour parity + claims-ledger; see PLAN R5) — docs, no new benchmark
0. START-OF-SESSION STEP 0 above: commit the pending reviewer plan-dir docs first.
a. Read PLAN R5. Update SCRATCHPAD §2 for R5.
b. Parity check (D-05): confirm R2/R3/R4 decoupled runs used the SAME protocol as the frozen P0b baseline
   — 200 seeds, 9 archetypes, 3 bands, held-out scoring (5 train / 4 held), Holm (primary) + BH (reported),
   bootstrap Δ-CIs. The decoupled JSONs already stamp this; just confirm and record. (Parity is on the
   PROTOCOL, not dataset identity — decoupled is its own DGP/hash by design.)
c. Append a section to `research/doc/2026-06-18-pillar-a-report-claims-and-caveats.md` with these honest
   framings, each tied to its result JSON (Holm-surviving only):
   - CALIBRATION (R2): the `planned` reference changed source (slot chunk → material throughput) but the
     SIGNAL SHAPE is identical (D8); the `enriched_shrink`/`enriched_dual_prior` win TRANSFERS and HOLDS
     (context-pred 12/12, recovery 8/12). Frame as "holds," NOT "decoupling improves calibration" (different
     DGP). Caveat: `max/night_owl` recovery loss; partials INCLUDED (D8a) → external-validity caveat.
   - DETECTION (R3): under the decoupled cadence the result is MORE CUSUM-favoring vs A4/P0b — step's
     `page_hinkley` edge disappears (step→cusum); only `fading_flame` drift retains non-CUSUM Holm wins.
     State vs A4/P0b, NOT vs a pure-null.
   - ETA (R4, headline): `gp_plus_analytic` beats `gp_ard` (held-out+Holm) on the SMALL band ONLY (cold
     start); worse on max; `analytic_required_rate` never wins. So #3 is QUALIFIED — a cold-start fallback
     layered on GP, NOT a GP replacement; `gp_ard`/`conformal` remain better on data-rich plans and
     conformal is the coverage fix. Claim only to this strength.
d. Write a short comparability doc under `research/doc/` (frozen vs decoupled headline numbers per track).
e. Update the DECISIONS.md change log entry for #3: flip its status to **🟡 QUALIFIED** (cold-start /
   small-plan benefit proven; NOT a general win) — do not mark it locked/proven as a GP replacement.
f. Commit, fill VERIFICATION R5 Developer notes, flip R5 →🟡 (NOT ✅), update SCRATCHPAD §5/§8, report.

IMMEDIATE NEXT ACTIONS — R6 (N=1 real-data circularity guard; see PLAN R6) — do AFTER R5
a. Read PLAN R6 + `college/scope/research-tasklist.md` Phase 5. AVAILABILITY GATE FIRST: locate real N=1
   logged sessions (candidate's own data / `research/comparison/scripts/capture_evidence.py` output). If
   none exists, STOP and ask the user — do NOT fabricate real data. Update SCRATCHPAD §2.
b. Compute the partial-session throughput `active/(position×chunk)` from the real data and overlay it on
   the synthetic partial distribution (face-validity) — report in-family or flag.
c. Run the ETA candidates on the real burn-up curve; report finish-date error DESCRIPTIVELY only (N=1 —
   no significance, no algorithm-superiority claim).
d. Descriptive bounds only — NO synthetic parameter tuned to real data (circularity guard).
e. Write a short write-up stub under `research/doc/` (or college/) with EXPLICIT non-claims; check the
   relevant P5 boxes in `college/scope/research-tasklist.md`.
f. Commit, fill VERIFICATION R6 Developer notes, flip R6 →🟡 (NOT ✅), update SCRATCHPAD §5/§8, report.

After R5 + R6 both ✅: the plan's Definition of Done (§5) is met — post a final summary and stop.

DO / DON'T (task-specific traps — each with its why)
- DO keep `activeMinutes/plannedMinutes == latent pace` for every active event including partials
  (PLAN D-02) — because three harness readers consume that ratio directly and this is what makes the
  D8 calibration "transfer" argument hold with zero math change.
- DO implement the analytic ETA candidate in actual-minutes currency (PLAN R4 note) — DON'T
  double-count the throughput factor; the harness target is already in actual minutes.
- DO ensure one `r_star` entry per emitted event and every event `duration>0` (PLAN D-03) — because
  the runners zip sessions against `r_star` and will misalign/raise otherwise.
- DO use the registered candidate name `enriched_dual_prior` (carry-forward #1).
- DON'T edit the prereg file or the calibration reference ids (rule 3) — because it silently breaks
  A-series provenance and the dual-prior reference fit.
- DON'T overwrite the frozen P0 baseline result JSONs in the default dirs — decoupled runs go to a
  separate --out-dir.
- DON'T mark any "win" that isn't Holm-surviving, and DON'T present oracle candidates as method wins —
  they are upper bounds.

UNKNOWNS & NO-FABRICATION
- PLAN open questions OQ-1..OQ-3 (partial inclusion-vs-down-weight, frozen cadence params,
  COLD_START_N + interval recipe): implement the PLAN's recommended option, and RECORD the resolution +
  rationale in SCRATCHPAD §4. Do not silently pick something else. (OQ-4 parity is resolved: D-09 frozen
  9-archetype n5400 anchor.)
- If a fact is genuinely unknowable from the code or PLAN, DISCOVER it from the stamped JSON provenance
  as the PLAN instructs, or stop and ask — don't guess.
- If the harness cannot fully run in this sandbox (uv cache / dep block — escalation was needed for the
  full suite), still WRITE all code and tests, run what you can, and record exactly what executed vs
  authored-only. Never invent results.

OUTPUT / DELIVERABLE
- Code + tests + stamped result JSONs under research/ (new `decoupled` dataset; decoupled results in
  separate per-track out-dirs), and the new college/scope/decoupled-session-preregistration.md.
- SCRATCHPAD.md kept current every session; VERIFICATION.md "Developer notes" filled per phase with
  SHAs; phase markers at 🟡 (awaiting review).
- After each phase (and at hand-off), post a short results message back to me: phase, commit SHA, the
  headline numbers, any deviation, and what's queued next — so I can run the review against your
  scratchpad and VERIFICATION.md.

DEFINITION OF DONE (confirm all before declaring a phase complete)
[ ] Phase implemented to PLAN spec; PLAN §3 anchors verified against live code (discrepancies logged).
[ ] For R1+: ran on the NEW decoupled dataset with a SEPARATE --out-dir; frozen P0 baseline + A-series
    datasets untouched; PARAMS_VERSION_HASH + calibration reference ids unchanged.
[ ] Real headline numbers recorded in SCRATCHPAD §5; for R4, the explicit paired-Holm-vs-gp_ard
    verdict is stated; comparisons use `enriched_dual_prior` (not `dual_prior`).
[ ] Tests written; run-vs-authored-only status recorded honestly.
[ ] VERIFICATION.md "Developer notes" filled + marker 🟡 (NOT ✅); SCRATCHPAD §8 session entry appended;
    SCRATCHPAD §2 "Next actions" updated for the next session.
[ ] Results message posted back to the reviewer.
If any box is unchecked, the phase is NOT done — finish it. (Only the reviewer flips 🟡→✅.)
